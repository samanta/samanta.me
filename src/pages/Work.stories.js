import Work from "./Work";

export default {
  title: "Samanta/Pages/Work",
  component: Work,
};

const Template = (args) => <Work {...args} />;

export const Default = Template.bind({});
Default.args = {};
