import Mentoring from "./Mentoring";

export default {
  title: "Samanta/Pages/Mentoring",
  component: Mentoring,
};

const Template = (args) => <Mentoring {...args} />;

export const Default = Template.bind({});
Default.args = {};
