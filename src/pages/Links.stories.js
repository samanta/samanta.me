import Links from "./Links";

export default {
  title: "Samanta/Pages/Links",
  component: Links,
};

const Template = (args) => <Links {...args} />;

export const Default = Template.bind({});
Default.args = {};
