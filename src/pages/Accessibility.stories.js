import Accessibility from "./Accessibility";

export default {
  title: "Samanta/Pages/Accessibility",
  component: Accessibility,
};

const Template = (args) => <Accessibility {...args} />;

export const Default = Template.bind({});
Default.args = {};
