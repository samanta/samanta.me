import Component from "./Header";

export default {
  title: "Samanta/Components/Layout/Header",
  component: Component,
};

const Template = (args) => <Component {...args} />;

export const Default = Template.bind({});
Default.args = {};
